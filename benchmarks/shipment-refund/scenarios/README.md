# Shipment/refund Scenario v0 example

`refund-ambiguous-timeout.v0.json` references the committed deterministic
Fixture v0 document in `../fixtures`.

Its policy and expected-outcome references still use an all-zero SHA-256
sentinel. Those sentinels satisfy Scenario v0 reference syntax only and do not
claim the referenced content exists or was verified.
